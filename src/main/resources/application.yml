
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3305/ifs-employee-handler-db
    password: ifs
    username: ifs
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect